service: serverless-java
frameworkVersion: '3'

provider:
  name: aws
  runtime: java11

package:
  artifact: target/layer-demo-1.0.0.jar

functions:
  hello:
    handler: com.amazonaws.lambda.LambdaFunctionHandler::handleRequest
    layers:
      - {Ref: CommonLibsLambdaLayer}

layers:
  commonLibs:
    package:
      artifact: target/java-lamda-layer.zip